Docker Execution:

Local:
Pre-requisite: Run Desktop docker 

--    "docker:test": //runs whole suite on docker just like npm run test 
--    "docker:test:split" : //runs whole suite by splitting test cases equaly based on SHRAD Count provided in ENV file